<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bapocalypse.train.dao.TrickDao">

    <select id="createTrick" parameterType="trick">
        INSERT INTO trick(trickId, uid, tid, cid, seatId, startSid, endSid, date, state)
        VALUES (#{trickId}, #{uid}, #{tid}, #{cid}, #{seatId}, #{startSid}, #{endSid}, #{date}, #{state})
    </select>

</mapper>